apiVersion: v1
kind: Service
metadata:
  name: nodejs-service
  namespace: casopractico2
  labels:
    app: casopractico2
spec:
  type: LoadBalancer
  selector:
    app: nodejs-app
  ports:
    - port: 80
      targetPort: 3000
